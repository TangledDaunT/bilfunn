// Shared cached HTML contains no account data; resolve identity privately in the browser.
export const sessionNavigation = `document.querySelectorAll('a[href="/logg-inn"]').forEach(a=>{a.dataset.accountLink='true';});async function updateAccountLinks(){try{const r=await fetch('/api/session',{credentials:'same-origin',cache:'no-store'});const s=r.ok?await r.json():null;document.querySelectorAll('[data-account-link]').forEach(a=>{const loggedIn=s&&s.authenticated&&typeof s.username==='string';a.href=loggedIn?'/konto':'/logg-inn';a.textContent=loggedIn?s.username:'Logg inn';});}catch{}}updateAccountLinks();window.addEventListener('pageshow',e=>{if(e.persisted)updateAccountLinks();});`;
